package com.example.app.email.entity;
import com.example.app.email.entity.TokenType;

/**
 * Enum for different types of verification tokens
 */
public enum TokenType {
    EMAIL_VERIFICATION,
    PASSWORD_RESET,
    EMAIL_CHANGE
}
